services:
  coder:
    build:
      context: coder
      dockerfile: Dockerfile
    ports:
      - "7080:7080"
      # - "7443:7443"
    environment:
      CODER_HTTP_ADDRESS: "0.0.0.0:7080"
      # CODER_TLS_ADDRESS: "0.0.0.0:7443"
      # CODER_TLS_ENABLE: "true"
      # CODER_TLS_CERT_FILE: "/etc/ssl/certs/cmwcoder.pem"
      # CODER_TLS_KEY_FILE: "/etc/ssl/certs/cmwcoder.key"
      # CODER_TLS_CLIENT_AUTH: "none"
      # CODER_TLS_ALLOW_INSECURE_CIPHERS: "true"

      CODER_ACCESS_URL: "https://coder.cmwcoder.h3c.com"
      CODER_WILDCARD_ACCESS_URL: "*.coder.cmwcoder.h3c.com"
      CODER_DOCS_URL: "https://docs.cmwcoder.h3c.com"
      
      CODER_BLOCK_DIRECT: "true"
      CODER_DERP_SERVER_STUN_ADDRESSES: "disable"
      CODER_PG_CONNECTION_URL: "postgresql://${POSTGRES_USER:-username}:${POSTGRES_PASSWORD:-password}@database/${POSTGRES_DB:-coder}?sslmode=disable"
      # CODER_REDIRECT_TO_ACCESS_URL: "true"
      CODER_DISABLE_NETWORK_TELEMETRY: "true"
      CODER_UPDATE_CHECK: "false"
    # If the coder user does not have write permissions on
    # the docker socket, you can uncomment the following
    # lines and set the group ID to one that has write
    # permissions on the docker socket.
    group_add:
      - "119"
    volumes:
      - ./coder/plugins:/opt/terraform/plugins
      - /var/run/docker.sock:/var/run/docker.sock
      # Run "docker volume rm coder_coder_home" to reset the dev tunnel url (https://abc.xyz.try.coder.app).
      # This volume is not required in a production environment - you may safely remove it.
      # Coder can recreate all the files it needs on restart.
      # - coder_home:/home/coder
    depends_on:
      database:
        condition: service_healthy
  database:
    # Minimum supported version is 13.
    # More versions here: https://hub.docker.com/_/postgres
    image: "postgres:latest"
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-username} # The PostgreSQL user (useful to connect to the database)
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password} # The PostgreSQL password (useful to connect to the database)
      POSTGRES_DB: ${POSTGRES_DB:-coder} # The PostgreSQL default database (automatically created at first launch)
    volumes:
      - coder_data:/var/lib/postgresql/data # Use "docker volume rm coder_coder_data" to reset Coder
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-username} -d ${POSTGRES_DB:-coder}",
        ]
      interval: 5s
      timeout: 5s
      retries: 5
  marketplace:
    build:
      context: marketplace
      dockerfile: Dockerfile
    ports:
      - "7081:7081"
    volumes:
      - ./marketplace/extensions:/opt/marketplace/extensions
volumes:
  coder_data:
  coder_home:
